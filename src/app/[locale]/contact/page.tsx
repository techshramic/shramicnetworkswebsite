import { ContactPage } from "@/components/content-pages";
import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata("Contact", "Contact Shramic Networks for demos, partnerships, careers, and investor relations.", "/contact");
export default ContactPage;
