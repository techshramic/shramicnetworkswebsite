import { AboutPage } from "@/components/content-pages";
import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata("About", "Shramic Networks mission, values, founding story, and rural technology vision.", "/about");
export default AboutPage;
