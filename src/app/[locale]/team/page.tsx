import { TeamPage } from "@/components/content-pages";
import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata("Team", "Leadership, advisors, and team profiles for Shramic Networks.", "/team");
export default TeamPage;
