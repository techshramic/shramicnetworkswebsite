import { BlogPage } from "@/components/content-pages";
import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata("Blog", "Insights and field notes on rural technology and livelihood infrastructure.", "/blog");
export default BlogPage;
